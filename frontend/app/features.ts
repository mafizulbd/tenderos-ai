// Secondary collaboration/CRM modules (approval workflow, comments, tasks, vendor CRM,
// contracts) solve problems larger organizations have, not the solo bidder or small firm
// who is TenderOS's realistic first customer. Cut from the default MVP surface per the
// Phase 0 hardening pass so the product stays focused on find-tender -> win-bid. The
// backend routes and frontend components are untouched — flip this back to re-enable.
export const SECONDARY_MODULES_ENABLED = false;
